# ─── Hugging Face Spaces — lightweight API deps only ───────────────────────
# Heavy AI/automation deps (sentence-transformers, spacy, playwright) are
# installed at runtime only when used, thanks to lazy imports.

# Core runtime
python-dotenv>=1.1.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
httpx>=0.28.0
jinja2>=3.1.0

# API server
fastapi>=0.115.0
uvicorn>=0.34.0
python-multipart>=0.0.12

# Database (optional — JSON fallback when PG is unavailable)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.30.0
alembic>=1.14.0

# Task scheduler (for live jobs refresh)
apscheduler>=3.10.0

# CV parsing (regex-only when spacy unavailable)
pdfplumber>=0.11.0
python-docx>=1.1.0
